# Arrays are the most basic way to structure data

array = [1,2,3,4,5,6] # Technically speaking, this is a Python List, but you can call it as array in DSA context

# A simple Algorithm to retreive a value from array with a given index
def getNumber(index):
    print(array[index]);
